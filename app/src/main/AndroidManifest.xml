<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:label="Rentoir"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rentoir"
        android:icon="@drawable/ic_food_logo"
        android:roundIcon="@drawable/ic_food_logo">
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Rentoir.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <!-- Tambahkan konfigurasi untuk activity baru -->
        <activity android:name=".MenuListActivity" android:exported="false" />
        <activity android:name=".RentDetailActivity" android:exported="false" />
    </application>
</manifest>